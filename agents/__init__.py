"""Emma's agents — the cognitive layer that plans, decides and acts.

- router      → intent routing + the Pipeline service container
- reasoning   → multi-step planning and execution
- control     → runs capabilities (files, shell, web, git, docker, ...)
- memory      → stores and recalls episodes
- security    → kill switch, consent, network gate, status
- map         → resolves regions and surfaces the region intelligence dashboard
- self_improve→ reads and modifies Emma's own code
"""
