# ── Emma core runtime ────────────────────────────────────────────────
# Mirror of the `[project] dependencies` block in pyproject.toml —
# keep the two in sync when adding core dependencies.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
groq==1.6.0  # pinned: tested version (groq 1.x stream_options regression)
paho-mqtt>=2.0
cryptography>=42
beautifulsoup4>=4.12
python-multipart>=0.0.9
aiofiles==25.1.0  # pinned: tested version (validated against real aiofiles)
orjson>=3.9.10

asyncpg>=0.30

# ── Optional capability extras (uncomment to enable) ────────────────
# voice (local STT/TTS):     
vosk>=0.3.45
edge-tts>=6.1
sounddevice>=0.4.6
numpy>=1.26
# vision (mediapipe):       
mediapipe>=0.10
opencv-python>=4.9
numpy>=1.26
# browser automation:        
playwright>=1.44
# desktop control:           
pyautogui>=0.9.54